FROM python:3.9-slim

WORKDIR /app

COPY ensemble_requirements.txt .
RUN pip install --no-cache-dir -r ensemble_requirements.txt

COPY src/api/ensemble_api.py .
COPY data/datasets.json . 

EXPOSE 8000

CMD ["uvicorn", "ensemble_api:app", "--host", "0.0.0.0", "--port", "8000"]
